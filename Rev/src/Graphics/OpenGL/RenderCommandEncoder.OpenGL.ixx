module;

export module Rev.OpenGL.RenderCommandEncoder;

export namespace Rev {

    struct RenderCommandEncoder {

        void* thing = nullptr;
        
        RenderCommandEncoder() {

        }

        ~RenderCommandEncoder() {
            
        }
    };
};