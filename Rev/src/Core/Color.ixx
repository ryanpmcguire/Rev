module;

#include <cmath>

export module Rev.Core.Color;

export namespace Rev::Core {

    struct Color {
        float r = 0;
        float g = 0;
        float b = 0;
        float a = 0;
    };
};